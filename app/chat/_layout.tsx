/**
 * @fileoverview Layout component for chat screens
 */

import { Slot } from "expo-router";

/**
 * Chat layout component that wraps chat-related screens
 * @returns JSX.Element
 */
export default function ChatLayout() {
  return <Slot />;
}
